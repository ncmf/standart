{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fieldvaluetext</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fieldValueText.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ fieldValueText.value }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fieldValueText.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_fieldvaluetext_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_fieldvaluetext_edit', { 'id': fieldValueText.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
